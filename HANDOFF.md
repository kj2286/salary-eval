# HANDOFF — 수학비서 IT 직군 평가/등급 산정 앱

## 마지막 작업 (2026-08-28, 2차) — **연봉 제거 + 상대평가 + 직전 대비 증감** (배포 완료)
- 요청: "연봉금액은 입력하지 않아도 될 것 같아. 이건 HR만 알아야 되거든, 이 평가는 리더들이
  직원을 평가하는 것. 등급이 나오면 몇 % 인상하면 좋을지도. 상대평가 방식. 직전 대비
  몇 % 올라갔는지 내려갔는지."
- 공개 URL: https://kj2286.github.io/salary-eval/ · 커밋 `c3290f5` (+ 문구 정리 후속)

### 무엇이 바뀌었나
1. **연봉 금액 완전 제거** — 입력·표시·저장·내보내기 전부. 산출물은 등급 + 인상률(%)까지.
   - compa-ratio 2축 merit matrix → **등급별 인상률 밴드 1축**. compa 조정은 HR 단계로 이관.
   - `market.js` 의 보상 밴드 표, 밴드 편집 UI, 최저임금·밴드이탈 점검 삭제
     → 정책 화면에 "HR 이 확인할 것" 체크리스트로만 안내.
   - CSV/TSV/JSON 어디에도 금액 없음 (파일 공유 시 연봉 유출 방지).
2. **상대평가 배분** (`assignRelativeGrades`)
   - 순위 기반, 목표 분포 S10/A25/B45/C15/D5 를 최대잉여법으로 인원 환산
   - **동점자는 반드시 같은 등급** — 경계를 걸친 동점 그룹은 다수결로 한 등급에 몰아줌
     (전원 동점이면 전원 B)
   - **절대 가드**: 4.0 이상 최소 B / 2.5 미만 최대 C. clamp 후 **단조성 재강제**
   - 비교 집단 전체/직무별/레벨별 선택, 분포·가드 임계값 편집, 절대평가 모드도 유지
   - 분기 화면 등급은 **잠정값**(집단이 채워지면 바뀜)임을 UI에 명시
3. **직전 대비 증감** (`deltaOf`, `DeltaBadge`)
   - 분기: 직전 분기 대비 점수·변화율 (예 `+1.00 (+33.3%)`)
   - 연간: 직전 연도 확정 대비 점수·등급(`B → A`)·인상률(`+2.0%p`)
   - 요약 카드·직원 헤더·분기표·연간표·CSV 전부 반영
4. 캘리브레이션 패널 재작성 — 분포뿐 아니라 **가드 개입 건**과 **경계 근소차 쌍**을 노출
5. 추천 인상률과 다르게 확정하려면 **사유 메모 필수**(없으면 확정 차단)
6. 명부 배지가 현재 모드(분기/연간) 기준 등급을 따라가도록 수정

### 잡은 버그
- 상대평가 절대 가드가 상한만 걸어 **순위 역전** 발생 (2.40점 C, 2.20점 B).
  → 등급 인덱스 clamp 후 단조성 재강제로 해결. 8개 분포 케이스 전수 검증.

### 데이터 스키마
- `salary-eval:*:v4`. v2/v3 자동 마이그레이션.
- **v3→v4 는 금액 필드를 옮기지 않는다** (`currentSalary`/`baseSalary`/`newSalary`/`band`).
  등급·점수·인상률은 보존. **v3 키는 지우지 않으므로 원본 복구 가능.**

### 검증 증거
- `npm run build` 성공 (307KB js / 31KB css)
- Playwright 헤드리스 2스위트 **전항목 통과**
  - v4 스위트(24개): 금액 부재 4종(화면/폼/입력필드/저장소), 상대 배분(4명 → A/B/B/C, B 2명),
    인상률 밴드·순위·잠정 표시, 증감 +33.3%/-33.3%, 연간 확정 패널 금액 없음,
    승급 +10%p(6→16), 추천 이탈 차단→사유 입력 시 해제, 확정 기록·CSV 금액 부재,
    가로 오버플로 0 @1600/1280/820/500px
  - 마이그레이션 스위트(9개): v3→v4 금액 제거·점수/등급/인상률 보존·v3 원본 보존·
    직전 연도 대비 인식
- 라이브 검증: 200 · 신규 문구 존재 · 금액 관련 문구 0건
- 스크린샷 육안 확인 2건

## 멈춘 지점
- 없음 — 완료.

## 다음 할 일
- **사용자 확인 필요**: 인상 재원(기본 4.5%)과 목표 분포를 회사 확정값으로 조정
  (앱 헤더 → [상대평가 · 재원 4.5%] 버튼).
- 인원이 4명이면 상대평가 목표는 S0/A1/B2/C1 이 된다. 실제 조직 인원에서 분포가
  의도대로 나오는지 한 번 확인할 것.
- (선택) 리더가 여러 명이면 평가자별 관대화 비교(평가자 편향 지표) 추가 가능.
- (선택) HR 전용 화면을 별도로 두고 거기서만 금액을 다루는 분리 모드.

## 미해결 이슈 / 주의
- **등급은 비교 집단에 의존한다.** 집단(전체/직무별/레벨별)을 바꾸면 같은 점수라도 등급이 달라진다.
- 인원이 적을수록 상대평가의 의미가 약해진다 — 5명에서 "S 10%" 는 0~1명이라는 뜻.
- 이 앱은 법률 자문이 아니다. 금액 관련 법정 점검(최저임금 등)은 HR 단계에서 해야 한다.
- 시장 인상률 참고치는 2026-08 기준. **연 1회 갱신 필요** (`src/data/market.js` 의 `MARKET_MERIT`).
- Google Sheets 전송은 `no-cors` 라 성공 응답을 읽을 수 없음 → 시트에서 직접 확인.
- 데이터가 브라우저 localStorage 에만 있음. 공용 PC 주의, JSON 백업 권장.
- 배포 시 GitHub Pages CDN 이 index.html 을 ~1분 캐시함. 번들 해시로 반영 확인할 것.

---

## 이전 작업 (2026-08-28, 1차) — **평가·보상 체계 전면 개편 + 배포 완료**
- 요청: "연봉사이트 분석해서 분기별 디자이너·개발자 평가가 맞는지 유명 연봉 컨설팅사 IT직군
  기준으로 검토·재설계. 한국 실정에 맞게. 신입/주니어/시니어/리더 연차 고려. HR과 밀접하니 꼼꼼히."
- 공개 URL: https://kj2286.github.io/salary-eval/ (배포·검증 완료, 번들 `index-COM31HMc.js`)
- 커밋: `029ea1c` (개편) + `ea4fa33` (SalaryPanel 제거). main·gh-pages 모두 푸시 완료.

### 진단한 기존 설계 결함 (조사 근거는 README 참조)
1. **레벨 개념 부재** — 신입과 시니어를 동일 6항목·동일 기대치로 평가. 컨설팅사 프레임워크의
   1차 축(직무 난이도·영향 범위)이 통째로 없었음.
2. **분기마다 연봉 인상** — 분기 4회 × 5%를 명부에 반영하면 연 21.6% 복리. 실질적 버그.
3. **단순평균 채점** — 성과/역량 미분리, 가중치 없음.
4. **compa-ratio 미반영** — merit matrix 두 축 중 하나가 없음.
5. **평가자 관대화 통제 장치·예산 제약 없음**
6. **한국 실정 미반영** — 인상률 밴드 3~10%인데 2026 실제는 평균 4.6%.
7. **C등급 0% 동결의 법적 리스크에 대한 가드 없음**

### 적용한 프레임워크 (2026-08 웹 조사)
- Radford(Aon) Job Level P1~P6 → **L1 신입 / L2 주니어 / L3 미들 / L4 시니어 / L5 리드**
- Mercer IPE · WTW GGS · Korn Ferry Hay 공통 축(영향범위·모호함·책임)을 각 레벨에 명시
- core(전사 공통) + functional(직무 전문) 2층 역량 구조
- Merit Matrix (등급 × compa-ratio), 캘리브레이션 세션, 연 1회 merit cycle
- 한국 데이터: 2026 인상률 평균 4.6%(대기업 3.8/중견 4.5/중소 4.8), 개발자 연차별 평균
  (1년차 3,766 / 3년차 4,158 / 6년차 5,139 / 10년차 6,214 만원), UX디자이너 신입 3,045,
  2026 최저임금 시급 10,320원(연환산 25,882,560원)

### 주요 변경
- `src/data/levels.js`(신규) — 레벨 5단계, 도메인 가중치, 승급 인상률(L1→L2 +5% ~ L4→L5 +10%)
- `src/data/roles.js`(재작성) — 도메인 4종(성과/전문성/협업/리더십). 협업·리더십은 전사 공통,
  성과·전문성은 직무별. 리더십 문항은 `minLevel` 로 L2/L3/L4부터 등장 → 문항 수 L1 8개 ~ L5 11개
- `src/data/market.js`(신규) — 직무 4 × 레벨 5 보상 밴드, 최저임금, 시장 인상률
- `src/lib/grading.js`(재작성) — 가중합 채점, 5등급(S/A/B/C/D)+권장분포, compa-ratio,
  merit matrix(재원 비례 스케일링), 연간 롤업
- `src/lib/compliance.js`(신규) — 노동법 가드레일 (아래)
- 화면 분리: **분기 평가**(연봉 입력 없음) / **연간 보상 확정**(compa 게이지·merit 분해·승급)
- 신규 컴포넌트: LevelPanel, CriteriaGroups, CompensationPanel, CalibrationPanel,
  PolicyDialog(재원·밴드 오버라이드), AnnualTable
- 저장 스키마 v3 + v2 자동 마이그레이션

### 수정한 실질 버그 (E2E가 잡음)
- **승급 시 merit을 새 레벨 밴드로 계산** → 레벨 점프 이중 반영(17.5%여야 할 것이 19%).
  성과 인상은 **현재 레벨 밴드** 기준으로 고정.
- **재확정 복리** — 확정 후 명부 연봉이 인상분 포함이라 같은 해 재확정 시 또 인상됨.
  확정 기록이 있으면 그때의 `baseSalary` 를 기준으로 고정.

### 법적 가드레일 (`lib/compliance.js`)
- block: 인상률 음수(임금 삭감 — 근기법 §94, 대법 2023.5.11. 전합 / 개별 동의 사안),
  최저임금 연환산 미달, C·D 등급 근거 메모 누락
- warn: 평가 분기 2개 미만, 밴드 상·하한 이탈, 승급 없이 15%↑ 인상

### 검증 증거
- `npm run build` 성공 (297KB js / 33KB css)
- Playwright 헤드리스 2스위트 **전항목 통과**
  - 스위트1(26개): 레벨별 문항 8/9/10/11, L1 리더십 미표시, 가중채점(리더십만 2점→4.25/A),
    D등급 메모 없이 저장 차단, 2026 Q1~Q4 저장, 연간 등급 A(평균 4.00), 승급 +10%p(7.5→17.5),
    음수 인상률 차단·확정버튼 비활성, 확정→명부 반영(66,000,000→70,950,000),
    재확정 복리 없음, 캘리브레이션 분포, 가로 오버플로 0 @1600/1280/820/500px
  - 스위트2(15개): v2→v3 마이그레이션(전원 L2·legacy 표시·새 등급경계 재계산 4.33→S),
    재원 9%→매트릭스 2배, 밴드 오버라이드 저장·반영
- 라이브 검증: 200 · 신규 문자열 존재 · 구버전 문구 0건
- 스크린샷 육안 확인 2건(분기 평가 / 연간 보상 확정)

## 멈춘 지점
- 없음 — 완료.

## 다음 할 일
- **사용자 확인 필요**: 보상 밴드 기본값(공개 시장 자료 기반 참고치)을 수학비서 실제 재원·정책으로
  덮어쓸 것. 앱 헤더 [보상 정책] → [레벨별 보상 밴드] 에서 편집 가능.
- 재원(기본 4.5%)도 회사 확정 예산으로 조정.
- 기존 v2 데이터가 브라우저에 있다면 자동 이관되지만 **전원 L2** 로 들어오므로 레벨 재지정 필요.
- (선택) 항목 가중치를 직무별로도 다르게, 팀 평균 대비 상대 등급, 성과급(일시금) 트랙 분리.

## 미해결 이슈 / 주의
- 이 앱은 법률 자문이 아니다. 가드레일은 전형적 분쟁 지점에서 멈춰 세우는 장치일 뿐.
- 밴드·인상률 기본값은 2026-08 시점 공개 자료 기준. **연 1회 갱신 필요**
  (`src/data/market.js` 의 `MID_MAN`, `MINIMUM_WAGE`, `MARKET_MERIT`).
- Google Sheets 전송은 `no-cors` 라 성공 응답을 읽을 수 없음 → 시트에서 직접 확인.
- 데이터가 브라우저 localStorage 에만 있음. 공용 PC 주의, JSON 백업 권장.
- 배포 시 GitHub Pages CDN 이 index.html 을 ~1분 캐시함. 번들 해시로 반영 확인할 것.
- Vercel 은 여전히 미사용(무료 플랜 Deployment Protection 때문). `salary-eval.vercel.app` 은 타인 프로젝트.

---

## 마지막 작업 (2026-08-16) — **외부 공개 배포 완료 (GitHub Pages)**
- 공개 URL: **https://kj2286.github.io/salary-eval/** (로그인 없이 누구나 접근, 검증 완료)
- 리포: https://github.com/kj2286/salary-eval (public, `main` = 소스 / `gh-pages` = 빌드 산출물)
- 재배포: `npm run deploy` (빌드 → gh-pages 워크트리 반영 → 푸시. `scripts/deploy-pages.sh`)
  ※ `dist/` 는 빌드마다 비워지므로 배포 이력은 git worktree 로 분리해 둠(nested .git 금지).
- `vite.config.js` 에 `base: './'` 추가 — 하위 경로(/salary-eval/) 서빙 대응.
- ⚠️ **Vercel 은 포기함**: `salary-eval-gangs-5471s-projects.vercel.app` 로 배포는 성공(READY)했으나
  Deployment Protection(Vercel Authentication)이 켜져 있어 외부인은 로그인 화면을 봄
  (302 → vercel.com/sso-api 로 확인). 무료 플랜에서 이 보호 해제가 유료 기능이라 GitHub Pages 로 전환.
  Vercel 프로젝트는 남아 있음(삭제하려면 대시보드에서 제거).
- ⚠️ `salary-eval.vercel.app` 은 **타인 프로젝트**(일본어 인사 시스템) — 우리 주소 아님, 혼동 주의.
- 검증: 공개 URL 200 + JS/CSS 200 + 헤드리스 렌더 정상(빈 상태 화면), 재배포 후 타이틀 갱신 확인.

## 마지막 작업 (2026-08-14, 2차) — **분기 평가 + 직원 명부 구조로 전환 완료**
- 요청: "평가항목은 매분기마다 할 수 있게 / 등록된 직원을 선택하는 UI로. 매번 새로 만들지 말 것."
- 데이터 모델 전환(v1 → v2): `employees`(명부) / `evaluations`(직원×분기 1건) / `drafts`(저장 전 편집분,
  `직원id::분기` 키) / `settings`(평가자·웹훅). 평가에는 이름·직무·연봉 **스냅샷**을 저장해 과거 분기 불변.
- 신규/개편 컴포넌트: QuarterSwitcher, EmployeeRoster(선택 UI), EmployeeDialog(등록·수정·삭제),
  EvaluationHistory(최근 6분기 추이), QuarterTable(미평가 포함 현황), ExportBar(이번 분기/전체 범위 토글).
  RoleTabs·RecordsTable 은 삭제(직무 선택은 직원 등록 시 1회).
- 부가 기능: `조정 후 연봉을 명부에 반영`(다음 분기 기준 연봉 갱신), 명부 검색·직무 필터,
  분기 진행률(평가 n/전체), 샘플 직원 4명 시딩.
- 검증 증거 (헤드리스 자동 플로우):
  - `npm run build` 성공(246KB js / 27KB css).
  - 미평가 직원 선택 시 FE 항목 노출·자동 인상률 3.5%(B), 전 항목 5점 → 5.00/S·자동 9%.
  - 저장 → `이프론/2026-Q3/avg=5/S/9%` 레코드 생성(총 4건), 분기 이동 시 Q2 이프론 미평가(3.00)·
    Q2 김디자 3.83 복원·Q3 김디자 4.67 — **분기별 독립 저장 확인**.
  - 명부 반영: 42,000,000 × 1.09 = 45,780,000 정확 반영.
  - 렌더 QA: 1600px/500px 스크린샷 정상, 가로 오버플로 없음.
- 아티팩트(단일 HTML) 재배포: https://claude.ai/code/artifact/f28de40b-226a-4171-9a4c-8ca664505205
- QA 도구: 시드/자동테스트 생성기는 스크래치패드 `make-seed.py` (`python3 make-seed.py test` → dist/seed-test.html).

## 1차 작업 (2026-08-14) — 신규 프로젝트 초기 구축 **완료**
- Vite + React 19 + Tailwind 4 + lucide-react 로 전체 구현. 백엔드 없음(localStorage).
- 구현 범위: 직무 4종 탭(PD/FE/BE/AI) × 6항목 5점 슬라이더 → 실시간 평균·등급(S/A/B/C)·추천 인상률 →
  연봉 계산(인상금액/조정 후 연봉/월 수령액 세전) → 평가 목록 표(합계 인상 재원) →
  CSV·JSON·TSV(시트 붙여넣기)·Apps Script 웹훅 전송.
- 검증 증거:
  - `npm run build` 성공 (231KB js / 25KB css).
  - 등급 경계 단위 확인: 4.5→S, 4.49→A, 3.8→A, 3.79→B, 2.8→B, 2.79→C.
  - 브라우저 자동 플로우(헤드리스): 저장→목록 반영, 슬라이더 조작→평균 2.67/C 동결 즉시 반영,
    직무 탭 전환→BE 항목 교체, 연봉 62,000,000×3.5%→64,170,000 정확.
  - CSV 이스케이프(콤마 포함 값) 및 직무 혼합 시 열 정렬 26열 일치 확인.
  - 렌더 QA 스크린샷: 데스크톱 1440px / 모바일 500px — 가로 오버플로 0 (scrollWidth == clientWidth).
- 미배포: git init·커밋·Vercel 배포는 하지 않음(요청 없음).

## 멈춘 지점
- 없음 (완료).

## 다음 할 일
- 사용자 확인 후: ① git init + 첫 커밋 ② Vercel 배포(Framework: Vite, output `dist`)
- 선택: 평가 항목 가중치, 팀 평균 대비 상대 등급, 예산 상한(총 인상 재원) 제약 기능.

## 미해결 이슈 / 주의
- Google Sheets 전송은 `no-cors` 라 성공 응답을 읽을 수 없음 → 시트에서 직접 확인 필요.
  확실한 경로가 필요하면 "시트용 복사"(TSV) 사용.
- 데이터가 브라우저 localStorage 에만 있으므로 공용 PC 사용 시 주의(개인정보·연봉 데이터).
